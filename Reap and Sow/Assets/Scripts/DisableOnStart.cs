using UnityEngine;

public class DisableOnStart : MonoBehaviour
{
    void Start()
    {
        //disable the game object on start. (USed for menu objects so they initialize but dont display
        //gameObject.SetActive(false);
    }
}
